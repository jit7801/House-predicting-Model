# House-predicting-Model
# 🏠 House Price Prediction Project

This repository contains a machine learning project to predict house prices based on various features using a Random Forest Regressor.

## 📁 Project Files

* `house_price_dataset.csv`: The dataset used for training and testing the model.
* `Home_price_predictor.py`: A Python script containing the full ML pipeline (data loading, preprocessing, model training, prediction, and evaluation).

## 🚀 How to Run the Project

### Prerequisites

You need Python 3 installed. The script uses several popular data science libraries. You can install them using pip:

```bash
pip install pandas scikit-learn
